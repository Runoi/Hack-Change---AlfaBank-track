# 🔬 ML Pipeline: Обучение и Генерация Сабмитов

Здесь находится весь код для подготовки данных, обучения ансамбля моделей и создания файла для лидерборда.

## 🛠 Стек
*   **Models:** CatBoost (GPU), LightGBM (GPU), PyTorch (Tabular ResNet).
*   **Techniques:** Smart Imputation, Feature Engineering (Engel's Law, Credit Utilization), AutoML Blending (SLSQP optimization), Geometric Averaging.
*   **Config:** Pydantic-based configuration (`config.py`).

## 🏆 Ключевые особенности
*   **Smart Feature Engineering:** Использование экономических инсайтов (Закон Энгеля, индексы кредитной нагрузки, Lifestyle-маркеры).
*   **Robust Preprocessing:** Умное заполнение пропусков (разделение на "логические нули" и неизвестные значения), клиппинг выбросов.
*   **Model Zoo:**
    *   **CatBoost** (GPU, Tuned Depth=8)
    *   **LightGBM** (GPU, Dart/GBDT)
    *   **PyTorch ResNet** (Deep Tabular Network с OneCycleLR и Embeddings)
*   **AutoML Blending:** Автоматический поиск весов ансамбля через минимизацию WMAE на OOF-выборке (Out-Of-Fold).

## 📂 Структура проекта
```text
pipeline/
├── data/                  # Положите сюда исходные csv файлы
├── processed/             # Сюда сохраняются parquet файлы
├── submissions/           # Сюда сохраняются результаты и модели
├── CatBoost/              # Скрипты обучения CatBoost
├── LightGBM/              # Скрипты обучения LightGBM
├── NN/                    # Скрипты обучения Нейросети
├── config.py              # Центральный конфиг (пути, параметры)
├── prepare_data.py        # ETL и генерация фичей
├── run_pipeline.py        # Главный оркестратор
└── optimize_ensemble.py   # Скрипт блендинга
```
## 🚀 Запуск обучения (ML Pipeline)

### 1. Настройка окружения
Перед запуском необходимо создать виртуальное окружение и установить зависимости, чтобы не конфликтовать с системными библиотеками.

**Windows:**
```bash
cd pipeline
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

**Linux / macOS:**
```bash
cd pipeline
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Данные
Положите файлы `hackathon_income_train.csv` и `hackathon_income_test.csv` в папку `pipeline/data/`.
*(Если папки `data` нет — создайте её).*

### 3. Запуск Пайплайна (One-Click)
Скрипт `run_pipeline.py` выполняет полный цикл обучения без участия человека:

```bash
python run_pipeline.py
```

### 3. Запуск Пайплайна (One-Click)
Скрипт `run_pipeline.py` выполняет полный цикл:
1.  **ETL:** Чистит данные, генерирует 400+ фичей, сохраняет в Parquet.
2.  **Training:** Обучает 3 модели (CatBoost, LGBM, NN) на 5 фолдах.
3.  **OOF Generation:** Сохраняет предсказания на трейне для блендинга.
4.  **AutoML:** Подбирает идеальные веса ансамбля.

```bash
python run_pipeline.py
```
Результат: **`submissions/submission_AUTO_ENSEMBLE.csv`** — файл для отправки на платформу.

### 📊 Результаты (Local Validation)
*   **LightGBM:** ~61.4k WMAE
*   **CatBoost:** ~64.2k WMAE
*   **Neural Network:** ~72.9k WMAE
*   **Ensemble (AutoML):** ~61.9k WMAE (OOF)

---
**Артефакты для продакшена:**
После обучения в папке `submissions/` появятся файлы: `model_catboost.cbm`, `model_lgbm.pkl`, `model_nn_artifacts.pth`, `lgbm_artifacts.pkl`, `features.json`,`catboost_artifacts.pkl`. Их нужно скопировать в сервис.
