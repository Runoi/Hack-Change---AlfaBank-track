Alfa-Bank Hackathon: AI Income Prediction & Scoring

> **Команда:** Финтехи
> **Трек:** Моделирование дохода физических лиц

Комплексное решение для **предсказания подтвержденного дохода** клиентов и генерации **персонализированных финансовых предложений** (Next Best Action).

Система объединяет SOTA ML-ансамбль (Gradient Boosting + Deep Learning) и готовый к продакшену микросервис на FastAPI.

---

## 🌟 Ключевые особенности

### 🧠 1. Продвинутый ML-движок
*   **Ансамбль 3-х моделей:** Стэкинг **LightGBM**, **CatBoost** и **Deep ResNet** (PyTorch) для максимальной точности.
*   **Smart Feature Engineering:** 400+ признаков, включая экономические инсайты (Закон Энгеля, индексы кредитной нагрузки, Lifestyle-маркеры).
*   **AutoML Blending:** Веса моделей подбираются автоматически через минимизацию ошибки на OOF (Out-Of-Fold) выборке.
*   **Результат:** WMAE ~46k (Топ лидерборда).

### 💼 2. Бизнес-логика и Персонализация
*   **Offer Engine:** Алгоритм подбирает продукты на основе профиля риска и трат (Кредитные карты, Ипотека, Инвестиции, Travel).
*   **Explainable AI (SHAP):** Система объясняет каждое предсказание. Мы знаем, *почему* модель решила, что доход именно такой.
*   **Safety Net:** Гарантированная выдача офферов для любого сегмента клиентов (от Mass до VIP).

### 🚀 3. Production-Ready Архитектура
*   Упаковано в **Docker**.
*   Реализовано на **FastAPI** (асинхронный быстрый API).
*   Строгая типизация данных (Pydantic).

---

## 🏗 Структура репозитория

Проект разделен на изолированные модули (Monorepo):

| Папка | Назначение | Стек |
| :--- | :--- | :--- |
| **[`pipeline/`](./pipeline)** | **Лаборатория ML.** Обучение моделей, генерация фичей, валидация и блендинг. | Pandas, PyTorch, CatBoost, LGBM |
| **[`ml-service/`](./ml-service)** | **Бэкенд.** REST API сервис для инференса и бизнес-логики. | FastAPI, Docker, Uvicorn |
| **[`frontend/`](./frontend)** | **Фронтенд.** Веб-интерфейс для демонстрации. | React / Vue |

---

## 🚀 Быстрый запуск (Docker)

Самый простой способ запустить решение — поднять микросервис в контейнере.

### 1. Подготовка моделей
Так как веса моделей тяжелые, они не хранятся в Git. Если вы только что склонировали репозиторий, вам нужно либо обучить модели (см. `pipeline/`), либо поместить готовые артефакты в папку `ml-service/models/`.

Необходимые файлы:
*   `model_catboost.cbm`
*   `model_lgbm.pkl`
*   `model_nn_artifacts.pth`
*   `*_artifacts.pkl` (метаданные)

### 2. Запуск контейнера
```bash
cd ml-service
docker-compose up --build -d
```

### 3. Тестирование API
Сервис доступен по адресу: **http://localhost:8000**

*   📄 **Документация (Swagger):** [http://localhost:8000/docs](http://localhost:8000/docs)
*   🧠 **Анализ клиента:** `POST /predict`
*   🔍 **Демо по ID:** `GET /ClientAnalysis/{id}`

---

## 📊 Метрики качества

На отложенной выборке (Public Leaderboard) и локальной валидации (OOF):

| Метрика | Значение | Комментарий |
| :--- | :--- | :--- |
| **WMAE** | **~61 000** | Взвешенная абсолютная ошибка (OOF) |
| **MAPE** | **~35%** | Средняя ошибка в процентах (для сегмента Middle+) |
| **Spearman** | **0.67** | Высокая ранжирующая способность |

---

## 🛠 Установка для разработки (ML Pipeline)

Если вы хотите переобучить модели или добавить новые фичи:

```bash
cd pipeline
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 1. Подготовка данных
python prepare_data.py

# 2. Обучение и Блендинг
python run_pipeline.py
```

---

### 👥 Команда "Финтехи"

*   **Селезнёв Роман(Runoi)** — Data Scientist (ML Pipeline, Feature Engineering)
*   **Селезнёв Роман(Runoi)** — Backend Developer (FastAPI, Docker, Architecture)
*   **Арам Казарян** — Frontend Developer (UI/UX, Visuals)

Мы превращаем данные в деньги и лояльность клиентов. 🚀